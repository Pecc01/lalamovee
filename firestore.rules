rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tracking/{code} {
      allow read, write: if true;
    }
    match /tracking_data/{code} {
      allow read, write: if true;
    }
  }
}
